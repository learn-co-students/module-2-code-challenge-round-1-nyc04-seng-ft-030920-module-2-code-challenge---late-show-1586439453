class EpisodeValidator < ActiveModel::Validator

    # ran out of time merp
    
    # def validate(record)
    #     unless record.episode.count = 1
    #       record.errors[:episode] << "Each guest can only appear once per episode!"
    #     end
    #   end


end 